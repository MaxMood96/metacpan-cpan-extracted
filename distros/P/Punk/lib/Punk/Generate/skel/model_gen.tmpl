package {% app %}::Model::{% name %};

use Punk::Model;

table '{% table %}';

# Fields compile into create/update validation; rows are plain hashrefs.
# See Punk::Model for types and constraints.
field id => { type => 'integer' };

# A filter names fields and operators, an ordering names fields and
# directions, and a page continues from its `next` token:
#
#   $c->model('{% name %}')->search(
#       { id => { '>' => 0 } },
#       { order_by => [ id => 'desc' ], limit => 20, after => $token });
#   $c->model('{% name %}')->count({ ... });

1;
