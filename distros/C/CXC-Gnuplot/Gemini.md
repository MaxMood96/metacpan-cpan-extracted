# About this project

* The project is written in Perl
* It uses the Object::Pad class library
* The VCS is mercurial
* The environment is controlled via plx
* precede all commands with 'plx'
* uses Dist::Zilla to manage build

# Do not do these things

* install additional Perl modules. stop if a module is missing
* do not commit code
